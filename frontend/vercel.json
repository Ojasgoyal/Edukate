{
  "rewrites": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "(?<subdomain>.*).yourdomain.com"
        }
      ],
      "destination": "/api/subdomain?subdomain=:subdomain"
    }
  ]
}
